services:
  voxtral_realtime_stt_worker_template:
    extends:
      file: ../../../model.base.yml
      service: base_worker_template
    image: "ukbgpt-stt-worker-voxtral:local"
    build:
      context: ../../../..
      dockerfile: images/vllm_worker/Dockerfile
    entrypoint:
      - /usr/local/bin/active_isolation_monitoring_entrypoint.sh
      - vllm
      - serve
